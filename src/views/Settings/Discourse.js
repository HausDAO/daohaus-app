import React from 'react';
import { Box, Text } from '@chakra-ui/react';

const Discourse = () => {
  return (
    <Box pl={6}>
      <Text my={6}>
        There will be a link to this forum in your setting page. You will also
        see buttons in proposal pages and forms to add new forum topics for your
        proposals.
      </Text>
    </Box>
  );
};

export default Discourse;
