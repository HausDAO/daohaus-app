//This function is for data returned from the user-hub query.
//Takes in an object of networks with deeply nested dao objects
//and return a flat array of daos

//AFAIK, this will only work with the
export const extractDaosFromNetworks = (networks) => {};
